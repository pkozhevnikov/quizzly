type: object
properties:
  reason:
    $ref: /pubapi/schemas/error-reason.yaml
  clues:
    type: array
    items:
      type: string

definitions:
  error-reason.yaml:
    $ref: /pubapi/schemas/error-reason.yaml
