type: object
properties:
  authors:
    type: array
    minItems: 1
    items:
      $ref: /pubapi/schemas/person.yaml
  inspectors:
    type: array
    minItems: 1
    items:
      $ref: /pubapi/schemas/person.yaml
required:
- inspectors
- authors

definitions:
  person.yaml:
    $ref: /pubapi/schemas/person.yaml
